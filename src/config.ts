import { Config } from './shared/entities';

const config: Config = {
  items: [{
    id: "ghostfellows",
    title: "Ghost Fellows",
    media: "ghost.jpg",
    thumbnail: "ghost.jpg",
  }, {
    id: "matches",
    title: "Matches",
    media: "matches.jpg",
    thumbnail: "matches.jpg",
  }]
}

export default config;
