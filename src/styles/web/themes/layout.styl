@import '../../macros'
@import '../../vars'
@import 'buttons'

html, body, main, main .page
  width 100%
  min-height 100%
  box-sizing border-box

html, body
  display flex

main
  position relative
  max-width maxWidth
  margin auto
  background-clip border-box
  overflow hidden auto

  .page
    position absolute
    grid-area a
    max-width 100%
    flex-column()
    padding pad

