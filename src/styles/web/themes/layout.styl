@import '../../macros'
@import '../../vars'
@import 'buttons'

html
  min-height 100%
  display flex

main
  position relative
  max-width maxWidth
  height 100%;
  margin auto
  background-clip border-box
  box-shadow 0 0 1rem black

  .page
    position absolute
    max-width 100%
    height 100%;
    flex-column()
    padding s("calc(%s - 3px)", pad)

