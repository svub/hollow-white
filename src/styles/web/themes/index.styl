@import '../../vars'
@import 'texts'
@import 'layout'
@import 'options'

// for theme in themes
//   src = s("%s", unquote(theme))
//   @import src

html, body, main
  background var(--bgColor)
  color var(--fgColor)
  display flex
  width 100%
  min-height 100%

.overlay
  background var(--bgColor)
  color var(--fgColor)

@import 'start'
@import 'read'
@import 'overlays'
