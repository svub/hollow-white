@import 'vars'
@import 'texts'

for theme in themes
  src = s("web-%s", unquote(theme))
  @import src
// @import 'web-light-theme'
// @import 'web-dark-theme'


html, body, main
  background var(--bgColor)
  color var(--fgColor)
  display flex
  width 100%
  min-height 100%

